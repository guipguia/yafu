apiVersion: v1
kind: Service
metadata:
  name: {{ include "yafu.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "yafu.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yafu.selectorLabels" . | nindent 4 }}
